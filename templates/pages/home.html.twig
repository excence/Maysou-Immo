{% extends 'base.html.twig' %}

{% block title 'Agence Maysou_Immobilier' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>Agence Maysou-Immobilier</h1>
        Lorem ipsum dolor sit amet, consecture adiscping elit.
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <H5 class="card-title">
                                <!-- property.show -> convention de nommage personnel -->
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </H5>
                            <p class="card-text">{{ property.city }}({{ property.postalCode }})</p>
                            <div class="text-primary" style="font-weight: bold; font-size: 2rem;">{{ property.formattedPrice }} €</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}